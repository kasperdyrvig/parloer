<!DOCTYPE html>
<html lang="da">
<head>
    <meta charset="UTF-8">
    <meta name="robots" content="noindex,nofollow">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{metaTitle}} | Grønlandsk parlør</title>
    <link href="{{'/assets/css/style.css' | url}}" rel="stylesheet">
    <script src={{'/assets/js/customelements.js' | url}}></script>
    <meta name="theme-color" content="#F35060">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-title" content="Grønlandsk parlør">
    <link rel="apple-touch-icon" href="{{'/assets/graphics/gp300.png' | url}}">
    <link rel="manifest" href="{{'/manifest.json' | url}}">
</head>
<body>
    {{content | safe}}
    <div id="snackbar"></div>
    <script>
        if ('serviceWorker' in navigator) {
            window.addEventListener('load', () => {
                navigator.serviceWorker.register('/assets/js/sw.js')
                    .then((reg) => {
                        console.log('Service worker registered.', reg);
                    }).catch(err => {
                        console.log('Registration failed', err);
                    });
            });
        }
    </script>
    <script src={{'/assets/js/features.js' | url}}></script>
</body>
</html>